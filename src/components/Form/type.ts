export type TData = {
  image: string;
  name: string;
  link: string;
  description: string;
  notes: string;
};

export interface IItemsForm {
  image: string;
  name: string;
  link: string;
  description: string;
  notes: string;
}
